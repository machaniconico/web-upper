# Web Upper server settings

# PORT=4173
# WEB_UPPER_FETCH_TIMEOUT_MS=15000
# WEB_UPPER_RENDER_TIMEOUT_MS=30000
# WEB_UPPER_MAX_HTML_BYTES=5242880
# WEB_UPPER_MAX_REDIRECTS=5

# Keep this disabled in shared or hosted environments.
# Set to 1 only when you intentionally edit trusted localhost or private-network sites.
# WEB_UPPER_ALLOW_PRIVATE_URLS=0
